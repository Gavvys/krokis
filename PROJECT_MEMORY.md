# Project memory

## Durable conventions

- UPPERCASE root artifacts are global, constitutional references. Lowercase OpenSpec artifacts apply only to one proposed change.
- `PRODUCT.md` owns product intent; accepted OpenSpec specs own current, testable behavior; `ARCHITECTURE.md` and `DESIGN.md` own durable technical and experience conventions.
- `PROJECT_MEMORY.md` provides orientation and links. It does not override a canonical source.
- Shared OpenSpec skills live in `.agents/skills/`; `.agent/skills/` is supported as a legacy fallback.
- Use `ROADMAP.md` for commitment-based sequencing: Now, Queued, Exploring, and Parked.

## Established decisions

| Date | Decision |
| --- | --- |
| 2026-07-19 | Use OpenSpec's `spec-driven` workflow to propose and archive changes. |
| 2026-07-19 | Build Krokis CLI in Go with zero runtime dependencies. Serve dashboard via `go:embed`. |
| 2026-07-19 | Parse MDX wiki documents client-side using marked.js and custom native Web Components. |
| 2026-07-19 | Standardize wiki file names as upper-case `SNAKE_CASE` ending in `.mdx`. |
| 2026-07-19 | Standardize Agent Skills folder mapping structure under `.agents/skills/`. |
| 2026-07-19 | Parse quality files (JUnit XML, lint JSON) in the insights checker command. |
| 2026-07-19 | Embed RapiDoc visual web component to render interactive OpenAPI specs. |
| 2026-07-19 | Adopt Open Sans as primary dashboard interface font and JetBrains Mono for monospace. |
| 2026-07-20 | Adopt global constitutional reference artifacts (AGENTS.md, PRODUCT.md, ARCHITECTURE.md, DESIGN.md, ROADMAP.md, PROJECT_MEMORY.md) in repo root. |
| 2026-07-20 | Treat local OpenSpec changes as team-level flow items. Report WIP, age, cycle time, throughput, and planning health; do not infer DORA metrics, rank individuals, or claim planning health proves validation. |
| 2026-07-20 | Design decisions live in root `ARCHITECTURE.md`. The `consolidate-design-decisions` change proposed an `ARCHITECTURE.mdx` section, then was superseded by `remove-duplicate-architecture-wiki`, which keeps root `ARCHITECTURE.md` as the sole Architecture source and drops `ARCHITECTURE.mdx`. `consolidate-design-decisions` archived un-synced. |
| 2026-07-20 | Visualize OpenSpec change flow as a hand-written inline SVG Web Component (`web/components/ChangeFlowGraph.js`) on a per-change detail route (`#/changes/<change>`). Derive nodes from `change-flow-insights` plus a new additive `ArtifactMap map[string][]string` payload field; no new endpoint. List/graph mode toggle persists under `krokis.changeViewMode` in `localStorage`. |
| 2026-07-20 | Promote OpenSpec changes to a top-level sidebar section. Canonical routes are `#/changes` and `#/changes/<change>`; the deprecated `#/insights/flow` and `#/insights/flow/<change>` URLs are rewritten via `history.replaceState` at the top of `handleRoute`. The `FlowInsights` Web Component is renamed `Changes` (`<changes>` tag); `<flow-insights>` is kept as a deprecated alias. |
| 2026-07-20 | Split archived changes out of `#/changes` into a dedicated `#/changes/archived` page. The `Changes` Web Component grows a `mode` property (`active` or `archived`) that drives the table filter and row schema. The `Archived` sidebar link is hidden until at least one completed change is present in the local OpenSpec workspace. |
| 2026-07-20 | Custom element tag is `<change-list>`, not `<changes>`. The name `changes` is reserved as an HTML global attribute and `customElements.define` rejects it. The deprecated `<flow-insights>` alias uses a thin subclass (`FlowInsightsAlias extends Changes`) because the same constructor class cannot be registered under two tag names. |
| 2026-07-20 | Every dashboard Web Component extends `KrokisElement` (`web/components/_base.js`), a Template Method base class that owns shadow DOM setup, `data` and `mode` setters, the `themechange` listener, the `escape` helper, and the `render()` contract. Subclasses only override `render()`. Client-side routing in `web/app.js` is table-driven via a `routes[]` array; `handleRoute` is a single dispatcher. The `mountPage` facade writes a `section-card` shell and mounts a custom element by tag, replacing the per-page duplication. ~140 lines of frontend boilerplate removed. |
| 2026-07-20 | Backend helpers added. `internal/web/server.go` exposes `withConfig(cfg, w, r)` middleware that loads the config once per request and the four API handlers use it. `internal/cmd/helpers.go` exposes `loadConfigOrDie()` and the six CLI commands use it. `init.go` exposes `scaffoldFile(path, content, label)` and the three scaffolding sites use it. `doctor.go` runs a `[]check` table with `ok` / `warn` / `fail` statuses instead of inline checks. ~60 lines of backend boilerplate removed. |
| 2026-07-21 | Spec-to-code coverage: `internal/metrics/coverage.go` walks `openspec/specs/**/spec.md`, extracts identifiers (custom-element tags, route hashes, backticked symbols, quoted strings) per requirement, scans the workspace tree (excluding `openspec/`, `.git/`, `node_modules/`, `vendor/`, hidden dirs, `tmp/`, `.air/`), and classifies each requirement as `covered` / `partial` / `uncovered`. The `/api/insights` payload gains a `coverage` section. The dashboard `#/insights/coverage` route renders the `CoverageReport` Web Component (extends `KrokisElement`) via `mountPage`. Coverage is implementation evidence, not OpenSpec validation. |
| 2026-07-21 | `krokis init` is now idempotent. It fills missing scaffolding without overwriting existing files, prints `↻ Skipped <path>` for files it leaves alone, and `--verbose` adds directory-level output. After scaffolding, init auto-invokes `krokis doctor` and streams its output. The `krokis doctor` command stays available standalone. Use `--skip-doctor` to suppress the auto-invocation. The init logic lives in `runInit(verbose, skipDoctor bool) int` and the doctor logic in `runDoctorChecks() int`, so both `cobra.Command.Run` and any future callers can reuse the same code path. |
| 2026-07-22 | Consolidated Agent Skill. `krokis init` scaffolds a single `.agents/skills/krokis/` tree (`SKILL.md` plus `workflows/`, `references/`, `scripts/`) instead of the three legacy per-workflow skills. The skill content lives at `internal/cmd/skill_template/krokis/` and is embedded into the binary via `//go:embed all:skill_template/krokis` in `internal/cmd/skill_template.go`. `scaffoldAgentSkills` now walks the embedded FS and writes each file via `scaffoldFile`. The legacy `krokis-insights/SKILL.md` and `krokis-wiki/SKILL.md` are removed from the repo (existing installs keep them on disk until removed manually); `roadmap-coordination/SKILL.md` stays as the dashboard's roadmap skill. |
| 2026-07-22 | Tightened `.gitignore` from `krokis` to `/krokis` so the bare-name pattern stops swallowing `internal/cmd/skill_template/krokis/`. Caught while staging the consolidated skill template. |
| 2026-07-22 | Shared plan discipline. The Krokis skill ships `references/plan-discipline.md` with seven editorial rules (gate thoughtfully, research before drafting, decide hard-to-reverse bets first, keep the plan self-contained, plan-read-only until approved, single bottom Open Questions block, clarifying-vs-assume). The OpenSpec skill `SKILL.md` files under `.agents/skills/openspec-*/` each carry a one-line "Discipline" pointer that references the discipline document, so the discipline is a single source of truth and is not re-stated per skill. The Krokis skill `SKILL.md` carries a "Read order" block at the top that names the workflow files and the references in the order an agent should read them. |
| 2026-07-22 | Evidence vs. inference rule. Plan-discipline now requires every non-trivial claim in `proposal.md`, `design.md`, and `tasks.md` to be tagged as either Evidence (cited local artifact, spec section, online reference, or established convention) or Inference (the agent's own reasoning or assumption). Inline markers `[E: pointer]` / `[I: reason]` or grouped `## Evidence` / `## Inferences` subsections; one style per artifact. Bare statements with no pointer are not evidence and must be promoted to inference. |
