## 1. OpenSpec Config and Global References

- [x] 1.1 Update `openspec/config.yaml` to declare root-level references and rules
- [x] 1.2 Create `AGENTS.md` and `PRODUCT.md` in workspace root
- [x] 1.3 Create `ARCHITECTURE.md` and `DESIGN.md` in workspace root
- [x] 1.4 Create `ROADMAP.md` and `PROJECT_MEMORY.md` in workspace root

## 2. Pluralization and Fallback

- [x] 2.1 Git move `.agent/` directory to `.agents/`
- [x] 2.2 Update `roadmap-coordination` skill scaffolding and priority path mappings in `internal/cmd/init.go`
- [x] 2.3 Verify project compilation and execution of init, validate, and doctor checks
